{"id": "88868de5-32c0-4092-9834-240f4ba9385a", "code": "import math\n\ndef f(x, a=1.0, b=0.0, c=1.0):\n    # Enhanced Gaussian with tunable width, shift, and amplitude.\n    # 'a' controls the width (inverse variance), 'b' controls the horizontal shift,\n    # and 'c' scales the amplitude. This offers more flexibility than previous versions\n    # by allowing independent control over the peak's height.\n    # The function remains smooth, continuous, and numerically safe for x in [-5, 5]\n    # as long as 'a' and 'c' are positive.\n    # We've added 'c' to allow for amplitude scaling, which can be crucial for\n    # fitting various target functions.\n    return c * math.exp(-a * (x - b)**2)\n\ndef search_algorithm():\n    # For this problem, return f itself.\n    # OpenEvolve will mutate the f() body, not this function.\n    return f", "language": "python", "parent_id": "4ed961eb-9aac-4550-a647-1161ff7076de", "generation": 2, "timestamp": 1763537753.717243, "iteration_found": 16, "metrics": {"combined_score": 100.0, "I1": 100.0, "I2": 100.0}, "complexity": 0.0, "diversity": 0.0, "metadata": {"changes": "Full rewrite", "parent_metrics": {"combined_score": 100.0, "I1": 100.0, "I2": 100.0}, "island": 0}, "prompts": {"full_rewrite_user": {"system": "You are an expert in functional analysis, symbolic mathematics, and numerical optimization.\nYour task is to generate candidate Python functions f(x) that are:\n  - smooth, continuous, and numerically safe across x in [-5,5]\n  - simple, parametric, and tunable (e.g., f(x, a) = exp(-a*x*x))\n  - analytically meaningful, but not necessarily the exact optimal solution at first\n\nAvoid:\n  - divisions by zero, NaNs, or values larger than 1e6\n  - brute-force Monte Carlo search inside the function\n  - completely rewriting the initial solution trivially\n\nEncourage:\n  - exploring parametric families\n  - small mutations of parameters or simple function composition\n  - compact, elegant, and numerically evaluable expressions\n", "user": "# Current Program Information\n- Fitness: 100.0000\n- Feature coordinates: No feature coordinates\n- Focus areas: - Fitness unchanged at 100.0000\n\n\n\n# Program Evolution History\n## Previous Attempts\n\n### Attempt 3\n- Changes: Full rewrite\n- Metrics: combined_score: 100.0000, I1: 100.0000, I2: 100.0000\n- Outcome: Mixed results\n\n### Attempt 2\n- Changes: Full rewrite\n- Metrics: combined_score: 100.0000, I1: 100.0000, I2: 100.0000\n- Outcome: Mixed results\n\n### Attempt 1\n- Changes: Unknown changes\n- Metrics: combined_score: 100.0000, I1: 100.0000, I2: 100.0000\n- Outcome: Improvement in all metrics\n\n## Top Performing Programs\n\n### Program 1 (Score: 100.0000)\n```python\n# initial_program.py\n\ndef f(x, a=1.0):\n    # Parametric Gaussian family: always safe, finite\n    return math.exp(-a*x*x)\n\ndef search_algorithm():\n    # For this problem, return f itself.\n    # OpenEvolve will mutate the f() body, not this function.\n    return f\n\n```\nKey features: Performs well on combined_score (100.0000), Performs well on I1 (100.0000), Performs well on I2 (100.0000)\n\n### Program 2 (Score: 100.0000)\n```python\nimport math\n\ndef f(x, a=1.0, b=0.0, c=1.0):\n    # Enhanced Gaussian with tunable width, shift, and amplitude.\n    # 'a' controls the width (inverse variance), 'b' controls the horizontal shift,\n    # and 'c' scales the amplitude. This offers more flexibility than previous versions\n    # by allowing independent control over the peak's height.\n    # The function remains smooth, continuous, and numerically safe for x in [-5, 5]\n    # as long as 'a' and 'c' are positive.\n    return c * math.exp(-a * (x - b)**2)\n\ndef search_algorithm():\n    # For this problem, return f itself.\n    # OpenEvolve will mutate the f() body, not this function.\n    return f\n```\nKey features: Performs well on combined_score (100.0000), Performs well on I1 (100.0000), Performs well on I2 (100.0000)\n\n### Program 3 (Score: 100.0000)\n```python\nimport math\n\ndef f(x, a=1.0, b=0.0, c=1.0):\n    # Modified Gaussian with horizontal shift and amplitude scaling.\n    # This function now includes a parameter 'c' to control the amplitude,\n    # allowing for a wider exploration of the Gaussian family.\n    # The expression remains smooth, continuous, and numerically safe for x in [-5, 5]\n    # as long as 'a' and 'c' are positive.\n    return c * math.exp(-a * (x - b)**2)\n\ndef search_algorithm():\n    # For this problem, return f itself.\n    # OpenEvolve will mutate the f() body, not this function.\n    return f\n```\nKey features: Performs well on combined_score (100.0000), Performs well on I1 (100.0000), Performs well on I2 (100.0000)\n\n\n\n## Diverse Programs\n\n### Program D1 (Score: 100.0000)\n```python\nimport math\n\ndef f(x, a=1.0, b=0.0):\n    # Modified Gaussian with a potential shift\n    # The '-b' term introduces a horizontal shift, controlled by parameter 'b'.\n    # This creates a new parametric family while remaining smooth, continuous,\n    # and numerically safe for x in [-5, 5] as long as 'a' is positive.\n    return math.exp(-a * (x - b)**2)\n\ndef search_algorithm():\n    # For this problem, return f itself.\n    # OpenEvolve will mutate the f() body, not this function.\n    return f\n```\nKey features: Alternative approach to combined_score, Alternative approach to I1\n\n### Program D2 (Score: 100.0000)\n```python\nimport math\n\ndef f(x, a=1.0, b=0.0):\n    # A slightly modified Gaussian with a horizontal shift\n    # The parameter 'b' introduces a tunable shift, exploring a different aspect\n    # of the function's shape while maintaining smoothness and safety.\n    return math.exp(-a*(x-b)**2)\n\ndef search_algorithm():\n    # For this problem, return f itself.\n    # OpenEvolve will mutate the f() body, not this function.\n    return f\n```\nKey features: Alternative approach to combined_score, Alternative approach to I1\n\n## Inspiration Programs\n\nThese programs represent diverse approaches and creative solutions that may inspire new ideas:\n\n### Inspiration 1 (Score: 100.0000, Type: High-Performer)\n```python\n# initial_program.py\n\ndef f(x, a=1.0):\n    # Parametric Gaussian family: always safe, finite\n    return math.exp(-a*x*x)\n\ndef search_algorithm():\n    # For this problem, return f itself.\n    # OpenEvolve will mutate the f() body, not this function.\n    return f\n\n```\nUnique approach: Excellent combined_score (100.000), Excellent I1 (100.000), Excellent I2 (100.000)\n\n### Inspiration 2 (Score: 100.0000, Type: High-Performer)\n```python\nimport math\n\ndef f(x, a=1.0, b=0.0, c=1.0):\n    # A Gaussian function with tunable width ('a'), horizontal shift ('b'),\n    # and vertical scaling ('c'). This adds another tunable parameter\n    # while maintaining smoothness, continuity, and numerical safety.\n    # The parameter 'c' scales the overall amplitude of the Gaussian.\n    return c * math.exp(-a * (x - b)**2)\n\ndef search_algorithm():\n    # For this problem, return f itself.\n    # OpenEvolve will mutate the f() body, not this function.\n    return f\n```\nUnique approach: Modification: Full rewrite, Excellent combined_score (100.000), Excellent I1 (100.000)\n\n### Inspiration 3 (Score: 100.0000, Type: High-Performer)\n```python\nimport math\n\ndef f(x, a=1.0, b=0.0, c=1.0):\n    # Modified Gaussian with horizontal shift and amplitude scaling.\n    # This function now includes a parameter 'c' to control the amplitude,\n    # allowing for a wider exploration of the Gaussian family.\n    # The expression remains smooth, continuous, and numerically safe for x in [-5, 5]\n    # as long as 'a' and 'c' are positive.\n    return c * math.exp(-a * (x - b)**2)\n\ndef search_algorithm():\n    # For this problem, return f itself.\n    # OpenEvolve will mutate the f() body, not this function.\n    return f\n```\nUnique approach: Modification: Full rewrite, Excellent combined_score (100.000), Excellent I1 (100.000)\n\n# Current Program\n```python\nimport math\n\ndef f(x, a=1.0, b=0.0):\n    # A slightly modified Gaussian with a horizontal shift\n    # The parameter 'b' introduces a tunable shift, exploring a different aspect\n    # of the function's shape while maintaining smoothness and safety.\n    return math.exp(-a*(x-b)**2)\n\ndef search_algorithm():\n    # For this problem, return f itself.\n    # OpenEvolve will mutate the f() body, not this function.\n    return f\n```\n\n# Task\nRewrite the program to improve its FITNESS SCORE.\nThe system maintains diversity across these dimensions: complexity, diversity\nDifferent solutions with similar fitness but different features are valuable.\nProvide the complete new program code.\n\nIMPORTANT: Make sure your rewritten program maintains the same inputs and outputs\nas the original program, but with improved internal implementation.\n\n```python\n# Your rewritten program here\n```", "responses": ["```python\nimport math\n\ndef f(x, a=1.0, b=0.0, c=1.0):\n    # Enhanced Gaussian with tunable width, shift, and amplitude.\n    # 'a' controls the width (inverse variance), 'b' controls the horizontal shift,\n    # and 'c' scales the amplitude. This offers more flexibility than previous versions\n    # by allowing independent control over the peak's height.\n    # The function remains smooth, continuous, and numerically safe for x in [-5, 5]\n    # as long as 'a' and 'c' are positive.\n    # We've added 'c' to allow for amplitude scaling, which can be crucial for\n    # fitting various target functions.\n    return c * math.exp(-a * (x - b)**2)\n\ndef search_algorithm():\n    # For this problem, return f itself.\n    # OpenEvolve will mutate the f() body, not this function.\n    return f\n```"]}}, "artifacts_json": null, "artifact_dir": null, "embedding": null}